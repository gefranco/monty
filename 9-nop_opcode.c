#include <stdlib.h>
#include <stdio.h>
#include "monty.h"

/**
 *nop - Function nop doesn’t do anything.
 *@stack: Pointer to header of the stack.
 *@line_number: Line number of the file
 */

void nop(stack_t **stack, unsigned int line_number)
{
	(void) stack;
	(void) line_number;
}
